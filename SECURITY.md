# 安全策略 / Security Policy

## 报告问题

- **不要在公开 issue 中披露安全问题**。请使用 GitHub 的
  [私有漏洞报告](../../security/advisories/new) 功能，或邮件联系维护者：`278391795+henryxzzm16@users.noreply.github.com`。
- 请在报告中包含：受影响版本/提交、复现步骤、以及你观察到的现象。

## 适用范围

本项目是本地工具：所有代码都在你的手机上运行，**不发起任何网络请求**，
不收集、不上传任何数据，也不修改系统分区（无 root、不加载内核模块）。因此风险面较小，但以下几类值得报告：

| 类型 | 例子 |
|---|---|
| 本地权限/沙箱问题 | 桥程序以非预期方式访问超出 USB fd 范围的资源 |
| 命令注入 | `flash51.sh` / `build.sh` 中的参数拼接导致任意命令执行 |
| 状态文件问题 | `.ch340_pts` / `.ch340_bridge.pid` 被符号链接或路径穿越利用 |
| 依赖漏洞 | 本项目依赖的 pyusb / pyserial / stcgal / libusb 中的已知漏洞 |

## 不适用（请勿报告为安全问题）

- **硬件风险**：不共地、供电不稳、热插拔导致的损坏 —— 这是操作问题，见 README 的免责声明。
- **烧录失败 / 砖化**：STC89 系列可重复烧录，ISP 流程本身不会锁死芯片；
  烧录失败通常是接线或冷启动问题。
- **需要 root 的攻击场景**：本项目的前提就是"无 root"，root 场景不在威胁模型内。

## 依赖安全

本仓库不包含第三方二进制（包括 SDCC 快照）。依赖各自维护者的安全更新：
`pip install -U pyusb pyserial stcgal`、`pkg upgrade libusb`。

## 支持的版本

只有最新版本（当前 `1.0.x`）会收到修复。由于项目处于早期阶段，不维护旧分支。
